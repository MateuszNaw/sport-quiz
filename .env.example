# MongoDB Atlas connection string (required for users, stats, leagues, questions)
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?retryWrites=true&w=majority

# Optional: Anthropic API for live question generation
# ANTHROPIC_API_KEY=

# Optional: override the default session signing secret
# SESSION_SECRET=
